version: "3.9"

services:
  db:
    image: mysql:8.0
    container_name: vidanimal_db
    restart: always
    environment:
      MYSQL_DATABASE: vidanimal
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: ./servidor
    container_name: vidanimal_backend
    restart: always
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_URL: jdbc:mysql://db:3306/vidanimal?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      DB_USERNAME: root
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./cliente
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8080/vidanimal}
        VITE_IMGBB_KEY: ${VITE_IMGBB_KEY:-}
    container_name: vidanimal_frontend
    restart: always
    depends_on:
      - backend
    ports:
      - "80:80"

volumes:
  db_data:
