services:
  frontend:
    build:
      context: ./cliente
      args:
        VITE_API_URL: ${VITE_API_URL:-https://ayuda-animal-murcia-web.onrender.com}
        VITE_IMGBB_KEY: ${VITE_IMGBB_KEY:-}
    container_name: vidanimal_frontend
    restart: always
